on:
  schedule:
    # Runs automatically once a day at 4:00 AM UTC
    - cron: '0 4 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout latest repository code
      uses: actions/checkout@v4
      with:
        ref: ${{ github.event.repository.default_branch }}
        fetch-depth: 1

    - name: Set up latest Python environment
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Control D Sync Script
      env:
        TOKEN: ${{ secrets.CONTROL_D_TOKEN }}
        PROFILE: ${{ secrets.CONTROL_D_PROFILES }}
      run: |
        chmod +x sync.py
        python sync.py